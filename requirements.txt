openai>=1.84.0
Pillow>=10.2.0
playwright>=1.49.0
pydantic>=2.11.0
PyYAML>=6.0.1
gradio>=5.0.0
playwright-stealth>=0.1.0  # 反检测库（可选）

# 安装说明：
# 1. 安装基础依赖：pip install -r requirements.txt
# 2. 安装playwright浏览器：python -m playwright install chromium
# 3. 如需使用stealth功能，会自动尝试导入，如未安装也不影响基础功能

